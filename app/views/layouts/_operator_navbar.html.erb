<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container">
        <a class="navbar-brand" href="/">vengo-tracker</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
      
        <div class="collapse navbar-collapse justify-content-end" id="navbarSupportedContent">
            <ul class="navbar-nav">
                <li class="nav-item active">
                    <a class="btn btn-link" href="/">Dashboard</a>
                </li>
                <li class="nav-item">
                    <a class="btn btn-link" href="/simulator/machines">Web Simulator</a>
                </li>
                <li class="nav-item">
                    <div class="dropdown">
                        <button class="btn btn-secondary" type="button"
                            id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            <%= current_operator.name || "Edit Your Profile" %> <i class="fas fa-angle-down"></i>
                        </button>
                        <div class="dropdown-menu" aria-labelledby="dropdownMenuButton">
                            <%= link_to "Account Settings", operator_account_path(current_operator), class:"dropdown-item" %>
                            <div class="dropdown-divider"></div>
                            <%= link_to destroy_operator_session_path, class:"dropdown-item", method: :delete do %>
                                Log Out <i class="fas fa-sign-out-alt"></i>
                            <% end %>
                        </div>
                    </div>
                </li>
            </ul>
        </div>
    </div>
</nav>
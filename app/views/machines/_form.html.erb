<%= form_for(@machine, :html => { :multipart => true, :id => "machine_form" }) do |f| %>
    <div class="form-group">
        <div class="field">
            <label for="latitude">Operator</label>
            <p><%= current_operator.name %></p>
        </div>
        <div class="field">
            <label for="latitude">Latitude</label>
            <%= f.text_field :latitude, class:"form-control", placeholder:"latitude", value:"#{@machine.latitude if @machine}" %>
        </div>
        <div class="field">
            <label for="longitude">Longitude</label>
            <%= f.text_field :longitude, class:"form-control", placeholder:"longitude", value:"#{@machine.longitude if @machine}" %>
        </div>
    </div>
    <div class="actions">
        <%= f.submit class:"btn btn-success", data: { "disable-with": "Updating..." } %>
    </div>
<% end %>